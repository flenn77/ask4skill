# ---------- MySQL ----------
MYSQL_ROOT_PASSWORD=superroot
DB_HOST=mysql
DB_PORT=3306            # ⚠️ doit rester 3306 dans les containers
DB_NAME=ask4skill
DB_USER=ask4skill
DB_PASSWORD=ask4skillpass

# ---------- phpMyAdmin ----------
PMA_HOST=mysql
PMA_USER=${DB_USER}
PMA_PASSWORD=${DB_PASSWORD}

# ---------- MailDev (SMTP) ----------
MAILDEV_HOST=maildev
MAILDEV_PORT=1025

# ---------- App (JWT / URLs / SMTP) ----------
JWT_SECRET=your_jwt_secret
FRONT_URL=http://localhost:3000

# SMTP utilisé par auth-service pour register + reset
SMTP_HOST=maildev
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# (Optionnel) OAuth placeholders
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_CALLBACK_URL=http://localhost:5000/auth/discord/callback
STEAM_API_KEY=
STEAM_RETURN_URL=http://localhost:5000/auth/steam/return
